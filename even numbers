#pablo torres 1/13/2025
# Write a python function named "isEven" that takes one parameter named "num". 
# When called, it checks if that number is even
# If it is, it returns True, otherwise it returns False. Call the function twice-
#once passing it 8 as a parameter, and the other time passing 3, and print the results each time.

#function definition
def isEven(num):
    if num%2==0:
        return True
    
    else:
        return False
    
 

result = isEven(7) #funtion call
print("can this number be divided by with no remainder?:", result)
